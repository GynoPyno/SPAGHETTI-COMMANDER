# Branch temporaneo tracciamento-tutte-mod-temp: traccia TUTTE le mod
# presenti nella cartella (whitelist disattivata rispetto al branch
# principale, dove resta attiva — vedi sviluppo--autonomo-avamposti).

# Escludi file temporanei/backup
*.bak
